{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Iniciar o Chrome com Python Server",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}